# API
PORT=3500
AUTH_USERNAME=admin
AUTH_PASSWORD=admin
LOG_LEVEL=info

# Database (optional - in-memory storage used by default)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=caddy_manager
DB_USER=caddy
DB_PASSWORD=caddy

# Web
VITE_API_URL=/api
